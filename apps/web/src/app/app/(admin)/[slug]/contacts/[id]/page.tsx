export default async function ContactPage({ params: {} }: { params: { slug: string; id: string } }) {
	// const result = await getContactById({
	// 	teamSlugOrId: slug,
	// 	contactId: id
	// })

	// if (!result.success) {
	// 	return <></>
	// }

	return <></>
}
