export default async function FeedbackPage({ params: {} }: { params: { slug: string; id: string } }) {
	// const result = await getFeedbackById({
	// 	teamSlugOrId: slug,
	// 	feedbackId: id
	// })

	// if (!result.success) {
	// 	return <></>
	// }

	return <></>
}
